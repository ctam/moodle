name: Check for new Moodle 4.5 release

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  sync:
    uses: ./.github/workflows/sync-moodle-release-reusable.yml
    with:
      upstream_branch: MOODLE_405_STABLE
      target_branch: UCSFCLE_405_STABLE